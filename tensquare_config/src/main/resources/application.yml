server:
  port: 12000
spring:
  application:
    name: tensquare-config
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/money8888/tensquare-config.git
  rabbitmq:
    host: 127.0.0.1
management:
  endpoints:
    web:
      exposure:
        include: bus-refresh
